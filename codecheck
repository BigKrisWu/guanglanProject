
下面的Java片段提示 Avoid throw raw exception like 'RuntimeException' 
```
    public SftpClient SftpClient(String host, String userName, String password, int strPort) {
        init();
        try {
            session = sshClient.connect(userName, host, strPort).verify().getSession();
            session.addPasswordIdentity(password);
            session.auth().verify();
            int retryTime = 0;
            while (!session.isOpen() && ++retryTime <= 3) {
                Thread.sleep((long) retryTime * 30 * 1000);
                session = sshClient.connect(userName, host, strPort).verify().getSession();
                session.addPasswordIdentity(password);
                session.auth().verify();
            }
            SftpClientFactory factory = SftpClientFactory.instance();
            return factory.createSftpClient(session);
        } catch (Exception e) {
            throw new RuntimeException(e);
        }
    }
```
